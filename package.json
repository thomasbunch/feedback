{
  "name": "feedback",
  "version": "0.1.0",
  "type": "module",
  "description": "MCP server for GUI testing and feedback - gives Claude Code eyes and hands",
  "bin": {
    "feedback": "./build/index.js"
  },
  "files": [
    "build"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/index.ts",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "detect-port": "^2.1.0",
    "playwright": "^1.58.2",
    "tree-kill": "^1.2.2",
    "wait-on": "^9.0.3",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/wait-on": "^5.3.4",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
